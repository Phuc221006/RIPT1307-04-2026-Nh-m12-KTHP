import routeProps from "./routeProps";
if (true) {
  Object.entries(routeProps).forEach(([key, value]) => {
    const internalProps = ["path", "id", "parentId", "isLayout", "isWrapper", "layout", "clientLoader"];
    Object.keys(value).forEach((prop) => {
      if (internalProps.includes(prop)) {
        throw new Error(
          `[UmiJS] route '${key}' should not have '${prop}' prop, please remove this property in 'routeProps'.`
        );
      }
    });
  });
}
import React from "react";
export async function getRoutes() {
  const routes = { "index": { "path": "/", "id": "index", "parentId": "@@/global-layout" }, "@@/global-layout": { "id": "@@/global-layout", "path": "/", "isLayout": true } };
  return {
    routes,
    routeComponents: {
      "index": React.lazy(() => import(
        /* webpackChunkName: "src__pages__index" */
        "../../../src/pages/index.tsx"
      )),
      "@@/global-layout": React.lazy(() => import(
        /* webpackChunkName: "layouts__index" */
        "C:/web_ts_dai_hoc/RIPT1307-04-2026-Nh-m12-KTHP/frontend/src/layouts/index.tsx"
      ))
    }
  };
}
